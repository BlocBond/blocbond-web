/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'encrypted-tbn0.gstatic.com', 'lh3.googleusercontent.com', 'media.licdn.com', 'grandriverrocks.com'],
    },
};

export default nextConfig;
