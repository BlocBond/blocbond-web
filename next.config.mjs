/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'static.wixstatic.com', 'lh3.googleusercontent.com'],
    },
};

export default nextConfig;
